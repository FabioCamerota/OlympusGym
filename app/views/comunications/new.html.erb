<h1>Crea nuova comunicazione</h1>

<%= form_with model: @comunication do |form| %>
  <div>
    <%= form.label :description %><br>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.submit "Crea comunicazione" %>
  </div>
<% end %>