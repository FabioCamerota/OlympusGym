<style>
table, th, td {
  border: 1px solid black;
}
</style>

<% if current_user.is_admin? || current_user.is_gymClient? %>

<h1>Lista dei corsi a cui sei prenotato</h1>


 <table style="width:100%">
  <tr>
    <th>Nome</th>
    <th>Giorno</th>
    <th>Orario</th>
    <th>Durata</th>
    <th>Descrizione</th>
  </tr>

  <% @courses.each do |course| %>
  <tr>
    <td><%= course.nome %></td>
    <td><%= course.giorno %></td>
    <td><%= course.orario %></td>
    <td><%= course.durata %></td>
    <td><%= course.descrizione %></td>
  </tr>
  <% end %> 
 
</table>
<br>
<%= link_to "Torna ai corsi", courses_path %>
<br>
<br>


<% else %>
<h1>Il tuo account è stato bannato dagli amministratori di OlympusGym</h1>
<% end %>