<!DOCTYPE html>
<html lang="en">
  <head>
    <title>OlympusGym</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-F3w7mX95PdgyTmZZMECAngseQB83DfGTowi0iMjiWaeVhAn4FJkqJByhZMI3AhiU" crossorigin="anonymous">

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    
  </head>

  <body>
    <div class="container-fluid">

        <div class="navbar-wrapper">
          <div class="container navbar-outer-container">
            <nav class="navbar navbar-expand-lg navbar-light bg-light py-4">
              <div class="container-fluid navbar-inner-container">
                <a class="navbar-brand logo-name" href="/">
                  <%= image_tag("logo.png", :class=>"d-inline-block align-text-top", :width=>"40",:height=>"32") %>
                  OlympusGym
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                  <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                  <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                      <% if not current_user.nil? %>
                      <a class="nav-link" href="/courses">Corsi</a>
                      <% else %>
                      <a class="nav-link disabled" href="#">Corsi</a>
                      <% end %>
                    </li>
                    <li class="nav-item">
                      <% if not current_user.nil? %>
                      <a class="nav-link" href="/comunications">Comunicazioni</a>
                      <% else %>
                      <a class="nav-link disabled" href="#">Comunicazioni</a>
                      <% end %>
                    </li>
                    <li class="nav-item">
                      <% if not current_user.nil? %>
                      <a class="nav-link" href="/myreservations/index">Prenotazioni</a>
                      <% else %>
                      <a class="nav-link disabled" href="#">Prenotazioni</a>
                      <% end %>
                    </li>
                    <li class="nav-item">
                      <% if not current_user.nil? %>
                      <a class="nav-link" href="/myprofiles/<%= current_user.id %>">Profilo</a>
                      <% else %>
                      <a class="nav-link disabled" href="#">Profilo</a>
                      <% end %>
                    </li>

                    <li class="nav-item">
                      <% if not current_user.nil? %>
                      <%= link_to('Logout', destroy_user_session_path, :method => :delete, :class=>"nav-link") %>
                      <% else %>
                      <a class="nav-link disabled" href="#">Logout</a>
                      <% end %>
                    </li>

                   <!-- <% if not current_user.nil? and current_user.is_admin? %>
                    <li class="nav-item dropdown">
                      <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                      Amministrazione
                      </a>
                      <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="/comunications/new">Crea comunicazione</a></li>
                        <li><a class="dropdown-item" href="/courses/new">Aggiungi corso</a></li>
                        <li><a class="dropdown-item" href="/adm">Pagina admin</a></li>
                      </ul>
                    </li>
                    <% end %> -->

                    <% if not current_user.nil? and current_user.is_admin? %>
                      <li class="nav-item">      
                        <a href="/adm" class="btn btn-primary">Amministrazione</a>
                      </li>
                    <% end %>

                  </ul>
                </div>
              </div>
            </nav>
          </div>
        </div>




      <%= yield %>


      <div class="footer">
        

        <% if not flash.empty? %> 
        <hr>
        <div class="notification center">
          <h3>Notifiche:</h3>
          <% flash.each do |type, msg| %>
            <div>
              <%= msg %>
            </div>
          <% end %>
        </div>
        <% end %>

      </div>
      
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-/bQdsTh/da6pkI1MST/rWKFNjaCP5gBSY4sEBT38Q/9RBh9AH40zEOg7Hlq2THRZ" crossorigin="anonymous"></script>


  </body>
</html>
